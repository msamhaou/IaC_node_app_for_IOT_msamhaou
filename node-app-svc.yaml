apiVersion: v1
kind: Service
metadata:
  name: node-app-svc
  namespace: dev
spec:
  selector:
    app: node-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 30007
  type: NodePort
